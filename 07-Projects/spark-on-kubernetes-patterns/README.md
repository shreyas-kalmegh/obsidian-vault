# Spark on Kubernetes Patterns
Purpose: Best practices for running Spark applications on Kubernetes.

Guidelines:
- Use dynamic allocation carefully with executors as pods.
- Isolate heavy IO workloads and tune memory overhead.
- Use proper resource requests and limits.

Architecture.md
