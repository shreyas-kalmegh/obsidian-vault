# ETL Data Lineage Tracker
Purpose: Track lineage of datasets through ETL processes for audit and debugging.

Guidelines:
- Capture metadata at every pipeline step (job id, input files, transformation logic).
- Store lineage graph in a metadata store and provide a UI.
- Support downstream impact analysis.

Architecture.md
