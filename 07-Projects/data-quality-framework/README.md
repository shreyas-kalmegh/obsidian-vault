# Data Quality Framework
Purpose: Reusable framework to define and run data quality checks across pipelines.

Guidelines:
- Define checks as declarative rules (schema, null rate, bounds).
- Integrate with pipeline orchestration to block on failures.
- Support historical tracking of data quality metrics.

Architecture.md
