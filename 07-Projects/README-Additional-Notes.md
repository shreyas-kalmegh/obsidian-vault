# Additional Projects & Complete Code Files

This folder contains multiple Rust project templates with example `src/main.rs` files. Use `cargo init --bin` and add dependencies mentioned in code comments (polars, parquet, rdkafka, axum, tokio).
