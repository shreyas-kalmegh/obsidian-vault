# Rust CLI Tooling Suite
Purpose: Develop a collection of CLI utilities for log parsing, parquet inspection, and small data transformations in Rust.

Guidelines:
- Write composable CLI commands with good UX (subcommands, clear flags).
- Provide consistent logging and error handling.
- Publish crates internally with versioning.

Architecture.md
