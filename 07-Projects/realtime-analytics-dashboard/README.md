# Real-time Analytics Dashboard
Purpose: Infrastructure for near-real-time dashboards powered by streaming aggregation outputs.

Guidelines:
- Pre-aggregate metrics in streaming layer to reduce query cost.
- Expose APIs for dashboards with caching layers.
- Ensure low-latency writes and reads with appropriate storage.

Architecture.md
